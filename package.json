{
  "name": "pirate_rook_scorecard",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "deploy": "git push origin --delete gh-pages && git clean -xdf && npm install && npm run build && git add dist -f && git commit -m 'deploy' && git subtree push --prefix dist origin gh-pages && git reset --hard origin/main"
  },
  "dependencies": {
    "core-js": "^3.8.3",
    "register-service-worker": "^1.7.2",
    "swiper": "^8.0.7",
    "vue": "^3.2.13"
  },
  "devDependencies": {
    "@vue/cli-plugin-babel": "~5.0.0",
    "@vue/cli-plugin-pwa": "~5.0.0",
    "@vue/cli-service": "~5.0.0"
  }
}
